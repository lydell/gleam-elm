pub type History(model, msg) {
  TODO
}

pub fn empty(_: model) -> History(model, msg) {
  TODO
}

pub fn add(user_msg: msg, user_model: model, history: History(model, msg)) {
  TODO
}

pub fn size(_: History(model, msg)) -> Int {
  1337
}
